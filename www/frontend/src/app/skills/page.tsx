import styles from './skills.module.css'

export default function AboutPage() {
    return (
        <main>
            <h1>Skills Page</h1>
            <button className={styles.btn}>Get a new skill!</button>
        </main>
    );
}